; Applied by docker-entrypoint.sh when APP_ENV is anything other than
; "production" (default: local/dev). Re-validates on every request so an
; edited file is picked up immediately, at a small performance cost that's
; irrelevant locally.
opcache.validate_timestamps = 1
opcache.revalidate_freq = 0
