; Applied by docker-entrypoint.sh only when APP_ENV=production. Not
; re-validating timestamps means a `docker exec`-edited file on a running
; container is silently ignored until the next deploy - intentional in
; prod (deploys are image rebuilds, not live edits), wrong for local dev.
opcache.validate_timestamps = 0
